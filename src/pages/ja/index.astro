---
import Layout from '../../layouts/Layout.astro';
import NavBar from '../../components/NavBar.astro';
import MainContent from '../../components/MainContent.astro';
import UpdatesWindow from '../../components/UpdatesWindow.astro';
import RightSidebar from '../../components/RightSidebar.astro';
import ControlPanel from '../../components/ControlPanel.astro';
import { nav, main, updates, sidebar } from '../../i18n/content';
---

<Layout lang="ja">
  <div class="bg-video-container" transition:persist="bg-video">
    <video id="bg-video" autoplay muted loop playsinline class="bg-video">
      <source src="/videos/background.mp4" type="video/mp4" />
    </video>
  </div>

  <div class="site-grid">
    <div class="col-left">
      <NavBar content={nav.ja} />
      <ControlPanel current="ja" />
    </div>
    <div class="col-mid">
      <MainContent content={main.ja} />
    </div>
    <div class="col-right">
      <UpdatesWindow content={updates.ja} />
      <RightSidebar content={sidebar.ja} />
    </div>
  </div>
</Layout>
